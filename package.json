{
  "name": "llama-search-monorepo",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm -r build",
    "start": "concurrently \"pnpm --filter backend start\" \"pnpm --filter frontend preview\""
  },
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}